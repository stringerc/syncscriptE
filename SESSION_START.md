# Session start — read this first

Single place to resume without re-explaining the whole thread. Update **Current focus** and **Last session** when you ship or pivot.

## How we like to work

- **Ship related pieces in one coherent pass** when it’s the same feature (code + tests + MEMORY/SESSION when it matters).
- **Small diffs** — no drive-by refactors; match existing patterns.
- **No option menus** — if several follow-ups are obviously part of the same goal, bundle them unless the user narrows scope.
- **Verify before done** — run **`npm test`** when touching Nexus tools/voice/contracts; **`CI=true npm run build`** for risky UI/build changes.
- **After meaningful prod deploy** — run **`npm run verify:prod-build`** until **MATCH**, or trigger Actions **Verify production HTML build fingerprint** / **Production dashboard smoke**. Green **`npm test`** does not prove live HTML matches **`git`** (see **Deploy check** below).
- **Protected surfaces** — follow **`.cursor/rules/02-protected-files-never-touch.mdc`** (Nexus core files, energy hooks, auth routes listed there).

## Current focus

SyncScript dashboard and **Nexus** (App AI): voice + tools-backed UI (canvas, tasks, maps), Vercel `api/*`, Supabase Edge where applicable. Continuity lives in **`MEMORY.md`** (ops + decisions) and this file (session handoff).

## Product priority (this cycle)

**App AI tab document drag/drop is live** (2026-04-25, prod = `3a39980`). User can drop any text-extractable doc on the AI tab to use as Reference or Modify target. Open canvas auto-attaches for in-place revisions. Next: Settings → Integrations → Webhooks UI for Gap #1 (then ship the five `INTEGRATION_RECIPES` as n8n JSON templates). Replace this line when you ship or pivot.

## IDE discipline (Cursor)

- **MCP:** Pin versions in **Cursor Settings → MCP** where possible — **`integrations/research/CURSOR_IDE_EXCELLENCE_SYNCSCRIPT.md`**.
- **GitHub labels for skill triage:** **`npm run gh:labels`** once per repo (creates **triage** + **skills** for **`.github/ISSUE_TEMPLATE/skill_evaluation.md`**).

## Last session (update each time)

- **MCP + library parity (repo, not necessarily prod yet):** Edge `resources-library-routes` — `requireJwtOrPat` with `library:read` / `library:write`, shared ingest, `POST /resources/upload-json` (base64 ≤1 MiB), `email-self` JWT-only. Edge `social-productivity-routes` — new PATs default-include library scopes (rotate old PATs). MCP `integrations/cursor-syncscript-mcp/server.mjs` v1.3.0 — task field parity, `syncscript_week_snapshot`, library tools, `syncscript_log_activity` extras. Tests: `user-files-library-contract`, `verify-stdio.mjs` (tracked) + `npm run verify:cursor-syncscript-mcp`. Docs: MEMORY § MCP operating model, `integrations/research/MCP_PARITY_AND_ROADMAP.md`, INDEX row, MCP README. **`public/openapi.json`** — ProductivityEdge tag, `syncscriptPat` scope blurb, `/resources/*` paths including `email-self` JWT-only. **Operator:** `npm run deploy:edge:make-server`, rotate PAT, then verify script against live Edge.

- **Capture inbox prod (2026-04-27):** Remote **`supabase db push`** applied **`20260427120000_user_capture_inbox`**. **`npm run deploy:edge:make-server`** shipped **`capture-inbox-routes`** + **`calendar-hold-exec`**. Live **`node scripts/verify-edge-productivity-http.mjs`** → **`GET /capture/inbox` → 401** (no longer skip). **`npm run deploy:vercel:prod`** completed (prebuilt); confirm **`www.syncscript.app`** shows dashboard strip when pending rows exist. PAT: new mints include **`capture:*`**; existing tokens need revoke + recreate for inbox MCP tools.

- **Nexus LLM compat + executor bridge (2026-04-26):** `api/_lib/openai-compat-model-policy.ts` (Kimi `is_error`, o/GPT-5 token + sampling), wired into `ai-service` + `callAgentChat`. `GET /api/agent/llm-stack` + `GET/POST /api/agent/executor-bridge` (optional `NEXUS_EXECUTOR_BRIDGE_*`). BYOK order groq→openrouter. Docs `integrations/research/NEXUS_LLM_COMPAT_AND_EXECUTOR_BRIDGE.md` + MEMORY section. **`npm test` green** (155 + 7 tsx tests).
- **Agent LLM without BYOK (2026-04-26):** `resolveAgentLLMConfig` + Oracle `runner/llm.mjs` use **first configured platform env** (`NVIDIA_API_KEY` → `GROQ_API_KEY` → …), not NVIDIA-only. Error token `no_byok_and_no_platform_llm_key`. Startup grants playbook: `integrations/research/STARTUP_CLOUD_CREDITS_PLAYBOOK.md`. **`npm run verify:prod-build`** MATCH + **`verify:prod:nexus-user-smoke`** OK same session.
- **Playwright visual-quality gate (2026-04-26):** Tier-1 layout geometry (`e2e/dashboard-layout-columns.spec.ts`, Chromium + Firefox), viewport screenshots (`e2e/visual-snapshots.spec.ts` + committed `*-snapshots/*.png`), Axe on `[data-section=hero]` + guest `#main-content` (`e2e/a11y-axe.spec.ts`). Scripts: `npm run test:e2e:visual-quality`, `npm run test:e2e:visual-snapshots:update`, `npm run verify:post-deploy:checklist`. CI job **`visual-quality-e2e`** in `.github/workflows/ci.yml`.
- **Git hygiene + prod deploy (2026-04-26 evening):** Large dirty tree was **uncommitted Tier 0–1 work** (48+ files) plus **thousands of local-only** artifacts (Aider, Playwright PNGs, `nature-cortana-platform/`, root phase docs). Shipped **`920c0af`** (full stack + dashboard `md` three-column), **`a654c7d`** (missing Edge **`hermes-bridge` / `engram-bridge`** + CI **`check-bundle-size`**, **`lighthouserc.cjs`**, **`src/camera`**), **`deb745e`** (gitignore for planning exports + scratch). **`git status` clean** on `main`. **`npm test`** + **`CI=true npm run build`** green before each push. **`npm run deploy:vercel:prod`** → **`npm run verify:prod-build`** **MATCH** on `www.syncscript.app` vs **`deb745e`**.
- **Prod checklist + Vercel deploy resumed (2026-04-26):** Remote DB: **`supabase link`** + **`supabase db push`** applied **`20260426040000_user_onboarding_progress.sql`** (`user_onboarding_progress` + RLS + realtime). Edge: **`npm run deploy:edge:make-server`** (includes **`stripe-routes.tsx`** checkout `interval` + annual price env fallbacks). Vercel: **`npm run deploy:vercel:prod`** succeeded after a prior aborted run (~3m build). **`npm run verify:prod-build`** → **MATCH** on `www.syncscript.app` vs local **`9f6a238`**. CLI may warn *“Did you mean to deploy the subdirectory deploy?”* — confirm in Vercel that **`www.syncscript.app`** points at this project. If **`agent-runner-live`** cron goes red under strict probe, set repo Variable **`AGENT_RUNNER_LIVE_RELAXED=1`** until runner is healthy. **Cursor:** approval UI sometimes not visible; user workaround is stop/continue or reduce auto-approvals — product feedback for Cursor, not app code.
- **Sentry + PostHog fully live (2026-04-26 night, prod commit `eb7bcda`):** Both observability stacks signed in via Composio MCP. Vercel envs: `VITE_SENTRY_DSN`, `SENTRY_DSN` (= web/serverless DSN), `VITE_POSTHOG_KEY`, `VITE_POSTHOG_HOST`, `SENTRY_AUTH_TOKEN` (sensitive). Sentry org `quicksync` / project `javascript-nextjs` (id 4510642041651200). Two issue-alert rules created (free tier — metric alerts need Team plan): `16966203` (new issues + regressions) and `16966202` (error spike 100+/5min) — both email IssueOwners + ActiveMembers fallback. **`@sentry/vite-plugin` 5.2.0 wired** as last Vite plugin: when `SENTRY_AUTH_TOKEN` set it creates release named `SYNCSCRIPT_BUILD_SHA`, uploads source maps as artifact bundle (238 files for `eb7bcda7ea02`), deletes `.map` files post-upload, `sourcemap: 'hidden'` so end-users can't deobfuscate. **GitHub integration already active** on Sentry org — repo `stringerc/syncscriptE` linked (integration id `404203`), 20 commits auto-imported with the first release, suspect-commit identification works. Direct ingest smoke: Sentry envelope `200 OK`, PostHog `{"status":"Ok"}` accepted. The Composio Rube MCP did all the heavy lifting via `SENTRY_*` and `VERCEL_ADD_ENVIRONMENT_VARIABLE` tools.
- **Tier 1+2+3+5 roadmap pass (2026-04-25 late night, prod commit `b6e083b`):** Shipped 8 items in one push. **Sentry web + serverless** (lazy, no-op until DSN), **PostHog** (5 critical events wired: pricing_page_view, checkout_clicked, task_created with source attribution, attachment_dropped, voice_*). **Webhooks Settings UI** (Settings → Integrations) with 7 new actions folded into `/api/agent/[action]` (Hobby fn-cap), secret-displayed-once pattern, deliveries log + replay. **Five n8n recipe JSON templates** at `public/integrations/recipes/syncscript-{launch-ops,sales-followup,finance-control,support-escalation,agent-automation}.n8n.json` — surfaced as one-click downloads in the Webhooks tab. **Realtime tasks/calendar** via `event_outbox` postgres_changes + `UserRealtimeBus` component (multi-device sync, no AuthContext touch). **Settings → Activity** audit log unified `nexus_tool_audit` + `playbook_audit_events` with surface/outcome filters. **Voice bundle-split** (lazy import in AppAIPage). **Keyboard shortcuts overlay** (`?` opens). **MEMORY hygiene preamble** in MEMORY.md. Migration `20260426030000_realtime_user_event_bus.sql`. **Owner action (none block deploy):** add `VITE_SENTRY_DSN` / `SENTRY_DSN` / `VITE_POSTHOG_KEY` Vercel env vars when accounts are ready. **Out of scope** (need own session): streaming voice (protected files), Notion/iCal/GCal imports (vendor OAuth), SAML/SSO/i18n/Mobile (each multi-day).
- **Self-publishing tunnel URL (2026-04-25 night, prod commit `5b009b0`):** Solved the ephemeral-cloudflared-URL problem WITHOUT requiring a Cloudflare auth click. New `public.runner_endpoints` table (service-role RLS, migration `20260426010000`); Oracle systemd timer `nexus-agent-runner-watchdog.{timer,service}` reads the current `cf-agent-runner-quick` URL every 60s and upserts to Supabase if changed; Vercel `/api/agent/[action]` resolves URL from row at request time (60s warm cache, falls back to env). **Validated end-to-end:** killed + recreated tunnel → new URL detected in 3s → published to row → smoke test against new URL passed (2 steps, 7s, 3¢, correct summary). Zero manual intervention on cloudflared rotation. Optional named-tunnel upgrade (`nexus-agent-runner.syncscript.app`) documented in MEMORY but not required — the system is permanent without it.
- **Nexus Agent Mode FULLY LIVE end-to-end (2026-04-25 night, prod commit `058ff5a`):** Oracle ARM A1 (`syncscript-a1-retry-2`, 157.151.235.143) running `ghcr.io/stringerc/syncscript-nexus-agent-runner:latest` Docker container on `:18790`, exposed via Cloudflare quick tunnel. Ran two real end-to-end smoke tests: example.com title extract (2 steps, 8s, 3¢) and Wikipedia World Wide Web article (4 steps, 9s, 5¢). **Three runtime fixes in the same session** (kept image green via `agent-runner-image.yml` rebuild after each push): (1) `bringup.sh` self-install crashed when piped via `curl|bash` because `$0=bash`; (2) Playwright `storageState: <jsonString>` was treated as file path → ENOENT — must JSON.parse to object first; (3) `safety.gate()` blocked `goto example.com` because `currentUrl='about:blank'` had empty host and `siteIsBlocked` defaulted empty → blocked. Also flipped default NIM model to `meta/llama-3.3-70b-instruct` (text-only, reliable function-calling) — Llama 3.2 90B vision was returning prose instead of structured tool_calls under tool_choice:auto. Forced `tool_choice='required'` with graceful fallback. Added 3-consecutive-thoughts abort with named-model error. New ops files: `scripts/smoke-agent-run-e2e.mjs` (repeatable e2e driver; reusable for any user_id/goal), `deploy/nexus-agent-runner/ORACLE_BRINGUP_RUNBOOK.md` (paste-ready commands), `.github/workflows/agent-runner-live.yml` (nightly /v1/health probe, repo var `AGENT_RUNNER_LIVE_VERIFY=1` to enforce). Vercel envs `AGENT_RUNNER_BASE_URL` + `AGENT_RUNNER_TOKEN` set. **155 + 2 tests passing.** See **MEMORY.md § Nexus Agent Mode — runner deploy + verified live**.
- **Agent Mode runner deploy + voice dock + persistent contexts (2026-04-25 evening):** Shipped: GHCR-built multi-arch image (`.github/workflows/agent-runner-image.yml`) + idempotent `bringup.sh` for one-line Oracle deploy + `scripts/verify-agent-runner-live.mjs`. Voice portal docks to top-left (240×168, spring transition) when an agent run is active — `useActiveAgentRun` + `VoiceDockedFrame` (no edits to `VoiceConversationEngine.tsx`). Persistent browser contexts: Playwright `storageState` round-tripped through `vault.secrets` per user, with `clear_browser_context` + `disconnect_browser_site(p_hostname)` user-callable RPCs. New Settings → Agent → Connected sites UI lists hostnames + per-site disconnect + clear all. Migration `20260425220000_browser_contexts.sql`. Contract test `tests/agent-runner-deploy-contract.test.mjs` (17 asserts). **153 + 2 tests passing.** AGENT_RUNNER_TOKEN added to Vercel prod (matches NEXUS_PHONE_EDGE_SECRET); AGENT_RUNNER_BASE_URL pending Cloudflare Tunnel. See **MEMORY.md § Nexus Agent Mode — runner deploy**.
- **Nexus Agent Mode foundation (2026-04-25, prod 3a39980):** Full Phase 1→5 schema + Vercel `/api/agent/[action]` dispatcher + Oracle runner Docker stack + frontend (sidebar tabs, AgentRunStream overlay, BYOK + tier + history Settings tabs, ProjectFilterDropdown, FilesLibraryEmbed). NVIDIA NIM is the free-tier default; eight BYOK providers (OpenRouter, Gemini, OpenAI, Anthropic, Groq, xAI, Mistral, Ollama, custom_openai_compat) all use one OpenAI-compatible wire shape. Schema migration `20260425170000_agent_mode_foundation.sql`, contract test `tests/agent-mode-foundation-contract.test.mjs` (30 asserts). **136 + 2 tests passing.** Owner action: bring up Oracle runner per `deploy/nexus-agent-runner/README.md` and set `AGENT_RUNNER_BASE_URL` + `AGENT_RUNNER_TOKEN` on Vercel — runs queue cleanly until then. Library moved to Settings → Files (`/library` and `/dashboard/library` redirect). `nexus-post-call-summary` folded into `phone/[endpoint].ts` to keep Hobby ≤12 functions. See **MEMORY.md § Nexus Agent Mode**.


- **App AI drag/drop documents (2026-04-25):** Drag any text doc onto the AI tab → two-half overlay (Reference vs Modify). Reference = Nexus reads as context for this turn. Modify = loads into DocumentCanvas + Nexus rewrites in place via `update_document`. Multiple files, paperclip fallback, keyboard accessible. Open canvas auto-attaches as modify-target every send (no more "re-paste my doc to ask for edits"). Server caps + format mirrored client/server with parity contract test. New files: `src/utils/document-attachment.ts`, `src/hooks/useAppAiAttachments.tsx`, `src/components/nexus/AppAiDropzoneOverlay.tsx`, `src/components/nexus/AppAiAttachmentsBar.tsx`, `api/_lib/nexus-attachments.ts`. AppAIPage edits ~30 lines additive. 16 new contract tests. See MEMORY § App AI tab — drag/drop + paste documents.
- **Gap #1 PROVEN live (2026-04-24):** Full pipeline — synthetic `task.created` event → event_outbox → fanout_event_outbox → webhook_deliveries → pg_cron webhooks.flush → HTTPS POST with HMAC → webhook.site received + signature verified in one 1-min tick. 6 pg_cron jobs active. Fixes during bring-up: added `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` to Vercel prod, set `verify_jwt=false` on make-server-57781ad9 (project has legacy JWT disabled and publishable keys aren't JWTs — function does its own `x-nexus-internal-secret` auth). Operator runbook + smoke script (`scripts/smoke-outbound-webhooks.mjs`) in MEMORY.md § Gap #1 live end-to-end. Test suite: 90 + 2 passing, includes HMAC bitwise parity test (Deno `crypto.subtle` vs Node `crypto.createHmac`).
- **Event outbox + outbound webhooks (2026-04-23):** Shipped Gap #1 — per-user webhook subscriptions so n8n / Make / Zapier can consume SyncScript events. Migration `20260423160000_event_outbox_and_webhooks.sql` (3 tables + RLS + fanout/claim/finalize RPCs with SKIP LOCKED). Emit helper `api/_lib/events.ts` wired into `executeNexusTool` finish choke point. Edge dispatcher `supabase/functions/make-server-57781ad9/webhook-dispatcher.tsx` (HMAC-SHA256, 8s timeout, exponential backoff 60s→12h, DLQ at 10 attempts, auto-disable at 20 DLQ'd). Flush cron `20260423170000_pg_cron_webhooks_flush.sql`. Contract test `tests/event-outbox-and-webhooks-contract.test.mjs` (11 asserts). MEMORY.md § Event outbox documents operator bootstrap SQL.
- **Autonomy heartbeat (2026-04-23):** Shipped **pg_cron + pg_net** migration (`supabase/migrations/20260423150000_pg_cron_autonomy.sql`) with 5 `syncscript.*` jobs (concierge 1-min, phone-dispatch 2-min, process-emails 5-min, tts-slo 10-min, proactive-detect 15-min) + vault-backed `syncscript_autonomy_post`. **Concierge worker hardened** with `claim_next_playbook_runs` (FOR UPDATE SKIP LOCKED, 120s lease) — safe to run at 1-min cadence. Contract test `tests/pg-cron-autonomy-schedule.test.mjs` added to `npm test`. **Before flipping the migration on, operator must** populate Vault: `syncscript_cron_secret` (=Vercel `CRON_SECRET`) and `syncscript_nexus_edge_secret` (=`NEXUS_PHONE_EDGE_SECRET`) via `vault.create_secret(...)`. See **MEMORY.md § Autonomy heartbeat**.
- **Nexus voice orb / modal stacking (2026-04-23):** `Dialog` default z-lifted to **480/481**; voice-invoked Task / Event / LinkedCalendar modals now always `stackAboveVoiceShell`. Nexus orb rebuilt around the **Aether** palette (`public/nexus/aether-orb-reference.png`, `src/components/nexus/nexus-aether-palette.ts`): layered radial inner fill, tri-color glow (`#5ED7ED` / `#D82F89` / `#F4C470`), conic sheen layer, and elliptical rotated particle trail in `NexusVoiceSpeakingDust` (palette-driven with occasional star twinkles). `innerBaseStyle` refactored into a function (fixed earlier "called as function" bug).
- **ROI rituals (2026-04-17):** **`lighthouserc.cjs`** + **`npm run lighthouse:ci`** + **`.github/workflows/lighthouse-monthly.yml`**; **`verify-prod-build.yml`** job summary on success; **`skill-source-audit-weekly.yml`** triage checklist in summary; **`scripts/gh-ensure-labels.sh`** + **`npm run gh:labels`**; **MEMORY** / **SESSION_START** / **TOOLS** / **skill-evaluations** README updated (deploy habit, control panel, product priority placeholder, MCP pin reminder).
- **Cursor + MEMORY wiring:** Committed **`.cursor/rules/07-syncscript-app-knowledge.mdc`**, bootstrap points agents at it before full **`MEMORY.md`** read.
- **Prod deploy drift audit:** Local **`CI=true npm run build`** injects **`<!-- syncscript-build:<sha> -->`** in **`build/index.html`**; **live `www.syncscript.app` was missing that marker** while **`npm test`** and **`prod-human-smoke`** still passed — meaning **Vercel HTML is behind git**, not that the Vite plugin is broken. Added **`verify-prod-build`** messaging, scheduled GitHub Action **`.github/workflows/verify-prod-build.yml`**, and a **deploy-drift** step on **`production dashboard smoke`**. **Remediation:** deploy **`main`** to the production Vercel project; confirm **Settings → Git**; hard-reload / SW cache if needed.
- **Nexus voice (earlier commits):** Signed-in voice uses **`/api/ai/nexus-user`** + tools; artifact rail, canvas, map embed, **`update_document`** — details in **`MEMORY.md`** § Nexus Voice.

## Blockers

- **Vercel prod HTML drift (2026-04-16):** `git push origin main` succeeded (`ef743cd` on GitHub), but **`npm run verify:prod-build`** still failed — live `/` had **no** `<!-- syncscript-build:… -->` yet. Confirm **Vercel → Deployments** for a new build from `main`; fix **Settings → Git** if no deploy triggers; after green deploy, re-run **`verify:prod-build`** (expect **MATCH**).

## Optional: micro-handoff for fragile tasks

Paste 5 lines in chat: **Goal · Constraint · “Read MEMORY §X and SESSION_START.md” · Done definition.**

## Browser console (not always SyncScript)

Messages like **`[uwLogger]`**, **`uw.js`**, **`chrome-extension://`**, **`runtime.lastError`** (back/forward cache), or **`postMessage` target origin** usually come from **browser extensions** (e.g. accessibility overlays) or **DevTools**, not from this app’s bundle. To verify app-only errors, use a **clean profile** / **Incognito with extensions off** or filter the console by **`syncscript`** / **`localhost`**. Real app issues (e.g. **`speakingTimeoutRef`**) belong in **`src/`** and should be fixed in-repo.

## Deploy check (prod vs repo)

Builds inject **`<!-- syncscript-build:&lt;sha&gt; -->`** in `index.html` and **`window.__SYNCSCRIPT_BUILD__.sha`** at runtime (see `vite.config.ts`).

- **Automated:** from repo root, after deploy: `node scripts/verify-prod-build.mjs https://www.syncscript.app` — exits **0** only if the HTML comment’s SHA matches **`git rev-parse --short HEAD`** (override URL with `VERIFY_PROD_URL` or first arg).
- **GitHub Actions:** **Verify production HTML build fingerprint** (scheduled + `workflow_dispatch`) — same check in CI.
- **Broader live smoke:** **Production dashboard smoke** — fingerprint + Playwright **`test:e2e:dashboard-nav:prod`** against **`PLAYWRIGHT_BASE_URL`**.
- **Landing UX/perf (monthly ritual):** **Lighthouse landing (monthly)** — **`npm run lighthouse:ci`** locally (needs Chrome); workflow uploads **`.lighthouseci/`** artifact. Config: **`lighthouserc.cjs`** (targets align with **`.cursor/rules/04-perf-seo-gate.mdc`**).
- **Manual:** DevTools → Elements → view page source on `/` and search for `syncscript-build`, or console: `__SYNCSCRIPT_BUILD__`.
- **Vercel:** Project → **Settings → Git** must point at the same GitHub repo/branch you push (e.g. **`stringerc/syncscriptE`** + **`main`**). If production is wired to another repo, pushes here never ship.
- **Cache:** after deploy, hard reload with cache disabled or unregister the **service worker** once so `index.html` isn’t stale.
