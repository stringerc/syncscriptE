{
  "name": "syncscript",
  "version": "1.0.0",
  "description": "SyncScript - AI-powered productivity platform",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev",
    "preview": "cd client && npm run preview",
    "install": "cd client && npm install"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}