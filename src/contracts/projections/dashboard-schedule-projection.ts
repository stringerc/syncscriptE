/**
 * Dashboard schedule projection — re-export canonical API for contracts / imports.
 * Implementation lives in `src/utils/dashboard-schedule-demo.ts`.
 */
export {
  resolveDashboardScheduleProjection,
  listOpenUserTasks,
  type DashboardScheduleProjection,
  type DashboardScheduleDemoOptions,
} from '../../utils/dashboard-schedule-demo';
