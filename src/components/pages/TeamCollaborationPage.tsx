/**
 * TeamCollaborationPage - Redirects to new TeamPage
 * 
 * v2.1 - Now uses the new team system with TeamContext
 */

import { TeamPage } from './TeamPage';

export function TeamCollaborationPage() {
  return <TeamPage />;
}
