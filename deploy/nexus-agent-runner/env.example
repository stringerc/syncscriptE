SUPABASE_URL=https://kwhnrlzibgfedtxpkbgb.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
NVIDIA_API_KEY=
NEXUS_PHONE_EDGE_SECRET=
AGENT_RUNNER_TOKEN=
AGENT_RUNNER_PORT=18790
AGENT_RUNNER_MAX_CONCURRENCY=4
AGENT_RUNNER_BROWSER_TIMEOUT_MS=120000
# 0 = headless (default on Oracle Docker). 1 = visible Chromium — needs DISPLAY (Mac desktop or Linux + Xvfb/VNC).
AGENT_RUNNER_HEADED=0
# Optional: slow down actions in headed mode (ms) so a human can follow the cursor.
AGENT_RUNNER_SLOW_MO_MS=0
AGENT_RUNNER_POLL_INTERVAL_MS=5000
AGENT_RUNNER_HEARTBEAT_LEASE_SECONDS=300
